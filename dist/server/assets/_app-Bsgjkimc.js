import { a6 as useRouter, U as reactExports, L as jsxRuntimeExports, O as Outlet } from "./server-C-mhO3-H.js";
import { B as getPublicEnv, L as Link, am as useAccessibility, F as FONT_SCALE_STEPS, as as useSession, ap as usePermission, ac as supabase } from "./router-CQNpPKTf.js";
import { B as Button } from "./button-CL2ribwv.js";
import { u as useAiComposer, D as DEBUG_LOADING_STEPS, C as COMPOSER_LOADING_STEPS, b as applyFillActionsProgressive, h as highlightDebugField, A as AiComposerProvider } from "./ai-composer-fill-D9vCpgIX.js";
import { a as cn } from "./utils-8RO4xBwZ.js";
import { X } from "./x-PTE7pfQq.js";
import { a as createLucideIcon, u as useComposedRefs } from "./index-BCXce4eP.js";
import { S as Sparkles } from "./sparkles-DHOkbk18.js";
import { M as MessageCircle, a as Minimize2 } from "./minimize-2-DFvggNrE.js";
import { D as DropdownMenu, e as DropdownMenuTrigger, a as DropdownMenuContent } from "./dropdown-menu-TDkRGDsQ.js";
import { m as useDirection, c as Primitive, h as createContextScope, e as composeEventHandlers, u as useCallbackRef, p as useLayoutEffect2, C as Check } from "./Combination-0mr5ceEx.js";
import { P as Presence } from "./index-BtZSYFDf.js";
import { c as clamp } from "./index-BTHtlONk.js";
import { u as useAlertBadge, a as useAlertSystem, b as useFAB, A as AlertSystemProvider } from "./use-alert-system-D8a5dvHQ.js";
import { T as TriangleAlert } from "./triangle-alert-DNyNSMGi.js";
import { B as Bell } from "./bell-CHJOTGSd.js";
import { S as Settings } from "./notification-dual-service-Dg5qjcgN.js";
import { S as ShieldAlert } from "./shield-alert-DhpaJisv.js";
import { S as Save } from "./save-CAEFdBf-.js";
import { C as CircleArrowUp } from "./circle-arrow-up-DiIPxDHX.js";
import { C as CircleX } from "./circle-x-B2VW5FO4.js";
import { R as RefreshCw } from "./refresh-cw-C-qHpEi1.js";
import { S as Switch } from "./switch-DFor-phQ.js";
import { L as Label } from "./label-gGZxLhmE.js";
import { u as useUiPreferences, E as ExternalLink } from "./use-ui-preferences-D0OgTjTN.js";
import { C as ContribuyentesProvider } from "./use-contribuyentes-RfU6S4og.js";
import { N as NotificationsProvider } from "./use-notifications-Cho5N9S4.js";
import { S as Sheet, a as SheetContent, c as SheetHeader, d as SheetTitle } from "./sheet-ig3hkEOj.js";
import { C as ClipboardList } from "./clipboard-list-DxraUa1U.js";
import { C as Clock } from "./clock-CP2BAXcM.js";
import { M as Minus, S as Slider } from "./slider-D0JxtEiD.js";
import { S as Search } from "./search-BtuCzAtg.js";
import { E as Eye } from "./eye-D5KGPJND.js";
import { P as Plus } from "./plus-hhv-7k2K.js";
import { b as useEstudioConfig } from "./use-estudio-config-D8B-OB3s.js";
import { C as CircleAlert } from "./circle-alert-2akrATqN.js";
import { L as LayoutDashboard } from "./layout-dashboard-COl_5e37.js";
import { B as Building2 } from "./building-2-CmiHWfC1.js";
import { F as FileText } from "./file-text-CtZVMHg4.js";
import { S as Scale } from "./scale-Ca3znwPN.js";
import { U as Upload } from "./upload-DYDStTmx.js";
import { R as Receipt } from "./receipt-CM8eIC5d.js";
import { B as BookOpen } from "./book-open-CGYoIsaj.js";
import { B as BookMarked } from "./book-marked-j2LJJ-r_.js";
import { G as GitBranch } from "./git-branch-vA2gbaqF.js";
import { W as Wallet } from "./wallet-BYbEj6vf.js";
import { L as Landmark } from "./landmark-D11GLb5V.js";
import { F as FileSpreadsheet } from "./file-spreadsheet-X5P97eTk.js";
import { C as ChartColumn } from "./chart-column-PMwdu55G.js";
import { C as CircleUser } from "./circle-user-DhM349qA.js";
import { S as Shield } from "./shield-5qqZO4k4.js";
import "node:async_hooks";
import "node:stream/web";
import "node:stream";
import "./index-Cls-RASW.js";
import "./chevron-right-B3IcY8Nq.js";
import "./circle-Dw7bLpiL.js";
import "./notification-service-D0rUP-wd.js";
import "./tareas-service-YuWHZuEd.js";
import "./contribuyentes-service-ZRfErUKW.js";
import "./http-client-BAKcXjQw.js";
import "./index-DFMAIghZ.js";
import "./useQuery-0d8p6ted.js";
function useLocation(opts) {
  const router = useRouter();
  {
    const location = router.stores.location.get();
    return location;
  }
}
const __iconNode$7 = [
  ["path", { d: "M12 20v-9", key: "1qisl0" }],
  ["path", { d: "M14 7a4 4 0 0 1 4 4v3a6 6 0 0 1-12 0v-3a4 4 0 0 1 4-4z", key: "uouzyp" }],
  ["path", { d: "M14.12 3.88 16 2", key: "qol33r" }],
  ["path", { d: "M21 21a4 4 0 0 0-3.81-4", key: "1b0z45" }],
  ["path", { d: "M21 5a4 4 0 0 1-3.55 3.97", key: "5cxbf6" }],
  ["path", { d: "M22 13h-4", key: "1jl80f" }],
  ["path", { d: "M3 21a4 4 0 0 1 3.81-4", key: "1fjd4g" }],
  ["path", { d: "M3 5a4 4 0 0 0 3.55 3.97", key: "1d7oge" }],
  ["path", { d: "M6 13H2", key: "82j7cp" }],
  ["path", { d: "m8 2 1.88 1.88", key: "fmnt4t" }],
  ["path", { d: "M9 7.13V6a3 3 0 1 1 6 0v1.13", key: "1vgav8" }]
];
const Bug = createLucideIcon("bug", __iconNode$7);
const __iconNode$6 = [
  ["path", { d: "m16 17 5-5-5-5", key: "1bji2h" }],
  ["path", { d: "M21 12H9", key: "dn1m92" }],
  ["path", { d: "M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4", key: "1uf3rs" }]
];
const LogOut = createLucideIcon("log-out", __iconNode$6);
const __iconNode$5 = [
  ["path", { d: "M15 3h6v6", key: "1q9fwt" }],
  ["path", { d: "m21 3-7 7", key: "1l2asr" }],
  ["path", { d: "m3 21 7-7", key: "tjx5ai" }],
  ["path", { d: "M9 21H3v-6", key: "wtvkvv" }]
];
const Maximize2 = createLucideIcon("maximize-2", __iconNode$5);
const __iconNode$4 = [
  ["rect", { width: "20", height: "14", x: "2", y: "3", rx: "2", key: "48i651" }],
  ["line", { x1: "8", x2: "16", y1: "21", y2: "21", key: "1svkeh" }],
  ["line", { x1: "12", x2: "12", y1: "17", y2: "21", key: "vw1qmm" }]
];
const Monitor = createLucideIcon("monitor", __iconNode$4);
const __iconNode$3 = [
  [
    "path",
    {
      d: "M20.985 12.486a9 9 0 1 1-9.473-9.472c.405-.022.617.46.402.803a6 6 0 0 0 8.268 8.268c.344-.215.825-.004.803.401",
      key: "kfwtm"
    }
  ]
];
const Moon = createLucideIcon("moon", __iconNode$3);
const __iconNode$2 = [
  ["circle", { cx: "12", cy: "12", r: "4", key: "4exip2" }],
  ["path", { d: "M12 2v2", key: "tus03m" }],
  ["path", { d: "M12 20v2", key: "1lh1kg" }],
  ["path", { d: "m4.93 4.93 1.41 1.41", key: "149t6j" }],
  ["path", { d: "m17.66 17.66 1.41 1.41", key: "ptbguv" }],
  ["path", { d: "M2 12h2", key: "1t8f8n" }],
  ["path", { d: "M20 12h2", key: "1q8mjw" }],
  ["path", { d: "m6.34 17.66-1.41 1.41", key: "1m8zz5" }],
  ["path", { d: "m19.07 4.93-1.41 1.41", key: "1shlcs" }]
];
const Sun = createLucideIcon("sun", __iconNode$2);
const __iconNode$1 = [
  ["path", { d: "M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2", key: "1yyitq" }],
  ["circle", { cx: "9", cy: "7", r: "4", key: "nufk8" }],
  ["line", { x1: "19", x2: "19", y1: "8", y2: "14", key: "1bvyxn" }],
  ["line", { x1: "22", x2: "16", y1: "11", y2: "11", key: "1shjgl" }]
];
const UserPlus = createLucideIcon("user-plus", __iconNode$1);
const __iconNode = [
  [
    "path",
    {
      d: "M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.106-3.105c.32-.322.863-.22.983.218a6 6 0 0 1-8.259 7.057l-7.91 7.91a1 1 0 0 1-2.999-3l7.91-7.91a6 6 0 0 1 7.057-8.259c.438.12.54.662.219.984z",
      key: "1ngwbx"
    }
  ]
];
const Wrench = createLucideIcon("wrench", __iconNode);
function getAiApiBase() {
  const fromEnv = getPublicEnv("VITE_AI_API_URL");
  if (fromEnv) return fromEnv.replace(/\/$/, "");
  {
    console.error("VITE_AI_API_URL no está configurada en producción.");
  }
  return "http://localhost:8001";
}
async function sendAiChat(params) {
  const res = await fetch(`${getAiApiBase()}/api/chat`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      message: params.message,
      history: params.history.map(({ role, content }) => ({ role, content })),
      mode: params.mode,
      thinking_seconds: params.thinkingSeconds,
      page_context: params.pageContext
    })
  });
  const data = await res.json();
  if (!res.ok) throw new Error(data.detail ?? `Error HTTP ${res.status}`);
  return data;
}
const MODES = [
  { id: "ask", label: "Ask", enabled: true, hint: "Consultas BD + internet" },
  { id: "composer", label: "Composer", enabled: true, hint: "Rellenar formularios (sin guardar)" },
  { id: "debug", label: "Debug", enabled: true, hint: "Revisar y corregir lo que puso Composer" }
];
function thinkingMax(mode) {
  if (mode === "composer") return 45;
  if (mode === "debug") return 30;
  return 15;
}
function loadingSteps(mode) {
  return mode === "debug" ? DEBUG_LOADING_STEPS : COMPOSER_LOADING_STEPS;
}
function AiChatBubble() {
  const { registration, buildPageContext } = useAiComposer();
  const [open, setOpen] = reactExports.useState(false);
  const [mode, setMode] = reactExports.useState("ask");
  const [thinkingSeconds, setThinkingSeconds] = reactExports.useState(3);
  const [input, setInput] = reactExports.useState("");
  const [loading, setLoading] = reactExports.useState(false);
  const [loadingStep, setLoadingStep] = reactExports.useState(0);
  const [messages, setMessages] = reactExports.useState([
    {
      role: "assistant",
      content: "Hola, soy CONTAM AI. **Ask**: consultas. **Composer**: rellena campos. **Debug**: revisa y corrige lo que Composer puso. Tú siempre guardas."
    }
  ]);
  const bottomRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    if (open) bottomRef.current?.scrollIntoView({ behavior: "smooth" });
  }, [messages, loading, open, loadingStep]);
  reactExports.useEffect(() => {
    if (!loading || mode !== "composer" && mode !== "debug") return;
    const steps = loadingSteps(mode);
    const id = window.setInterval(() => {
      setLoadingStep((s) => (s + 1) % steps.length);
    }, 900);
    return () => window.clearInterval(id);
  }, [loading, mode]);
  reactExports.useEffect(() => {
    const max = thinkingMax(mode);
    if (thinkingSeconds > max) setThinkingSeconds(max);
  }, [mode, thinkingSeconds]);
  const appendStreamLine = reactExports.useCallback((msgIndex, line) => {
    setMessages((prev) => {
      const next = [...prev];
      const msg = next[msgIndex];
      if (!msg) return prev;
      next[msgIndex] = {
        ...msg,
        streamLines: [...msg.streamLines ?? [], line],
        content: [...msg.streamLines ?? [], line].join("\n")
      };
      return next;
    });
  }, []);
  async function runLiveFill(res, streamMsgIndex, kind) {
    const actions = res.fill_actions ?? [];
    if (!actions.length || !registration) {
      setMessages((prev) => {
        const next = [...prev];
        const msg = next[streamMsgIndex];
        if (msg) {
          next[streamMsgIndex] = {
            ...msg,
            streaming: false,
            content: res.reply,
            toolsUsed: res.tools_used
          };
        }
        return next;
      });
      return;
    }
    const delay = Math.max(80, Math.min(400, Math.floor(thinkingSeconds * 1e3 / actions.length)));
    const intro = kind === "debug" ? `
🔧 Corrigiendo **${actions.length}** campos que no cuadraban…
` : `
📝 Rellenando **${actions.length}** campos…
`;
    appendStreamLine(streamMsgIndex, intro);
    await applyFillActionsProgressive(
      actions,
      (action) => registration.applyFill(action),
      (action, i, total) => {
        const label = action.label || action.field_path;
        const preview = action.value.length > 42 ? `${action.value.slice(0, 42)}…` : action.value;
        const prefix = kind === "debug" ? "🔧" : "✏️";
        appendStreamLine(
          streamMsgIndex,
          `${prefix} \`${i + 1}/${total}\` **${label}** → ${preview}`
        );
      },
      delay,
      kind === "debug" ? highlightDebugField : void 0
    );
    setMessages((prev) => {
      const next = [...prev];
      const msg = next[streamMsgIndex];
      if (msg) {
        next[streamMsgIndex] = {
          ...msg,
          streaming: false,
          content: `${msg.content}

---

${res.reply}`,
          toolsUsed: res.tools_used
        };
      }
      return next;
    });
  }
  async function handleSubmit(e) {
    e.preventDefault();
    const text = input.trim();
    if (!text || loading) return;
    const needsForm = mode === "composer" || mode === "debug";
    if (needsForm && !registration) {
      setMessages((prev) => [
        ...prev,
        { role: "user", content: text },
        {
          role: "assistant",
          content: "⚠️ Abre el **Editor** de Ficha RUC para revisar o rellenar campos."
        }
      ]);
      setInput("");
      return;
    }
    setMessages((prev) => [...prev, { role: "user", content: text }]);
    setInput("");
    setLoading(true);
    setLoadingStep(0);
    const history = messages.filter((m) => m.role === "user" || m.role === "assistant");
    const maxThink2 = thinkingMax(mode);
    const think = Math.min(thinkingSeconds, maxThink2);
    const streamIndex = mode === "composer" || mode === "debug" ? messages.length + 1 : -1;
    if (mode === "composer" || mode === "debug") {
      const intro = mode === "debug" ? "🔍 Revisando datos rellenados vs base de datos…" : "⏳ Analizando formulario y fuentes de datos…";
      setMessages((prev) => [
        ...prev,
        {
          role: "assistant",
          content: intro,
          streaming: true,
          streamKind: mode,
          streamLines: [intro]
        }
      ]);
    }
    try {
      const pageContext = needsForm ? buildPageContext() : void 0;
      const res = await sendAiChat({
        message: text,
        history,
        mode,
        thinkingSeconds: think,
        pageContext
      });
      if (mode === "composer" || mode === "debug") {
        appendStreamLine(
          streamIndex,
          mode === "debug" ? "✅ Revisión completada." : "✅ Plan recibido del servidor."
        );
        await runLiveFill(res, streamIndex, mode);
      } else {
        setMessages((prev) => [
          ...prev,
          { role: "assistant", content: res.reply, toolsUsed: res.tools_used }
        ]);
      }
    } catch (err) {
      const msg = err instanceof Error ? err.message : "Error desconocido";
      if ((mode === "composer" || mode === "debug") && streamIndex >= 0) {
        setMessages((prev) => {
          const next = [...prev];
          if (next[streamIndex]) {
            next[streamIndex] = {
              role: "assistant",
              content: `⚠️ ${msg}. ¿Servidor AI activo? (cd ai-agent/server → python main.py)`,
              streaming: false
            };
          }
          return next;
        });
      } else {
        setMessages((prev) => [
          ...prev,
          {
            role: "assistant",
            content: `⚠️ ${msg}. ¿Servidor AI activo? (cd ai-agent/server → python main.py)`
          }
        ]);
      }
    } finally {
      setLoading(false);
    }
  }
  const activeMode = MODES.find((m) => m.id === mode);
  const maxThink = thinkingMax(mode);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    open && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "fixed right-5 bottom-20 z-50 flex w-[min(420px,calc(100vw-2.5rem))] h-[min(580px,calc(100vh-6rem))] flex-col overflow-hidden rounded-2xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 shadow-2xl",
        role: "dialog",
        "aria-label": "CONTAM AI Chat",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "flex items-center justify-between bg-slate-900 dark:bg-slate-950 px-4 py-3 text-white", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold", children: "CONTAM AI" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "rounded-full bg-slate-700 dark:bg-slate-800 px-2 py-0.5 text-[10px] font-medium", children: [
                "Beta · ",
                activeMode.label
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                type: "button",
                onClick: () => setOpen(false),
                className: "rounded-lg p-1 text-slate-300 hover:bg-slate-700 hover:text-white",
                "aria-label": "Cerrar chat",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "size-4" })
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-1 border-b border-slate-100 dark:border-slate-700 bg-slate-50 dark:bg-slate-800 px-3 py-2", children: MODES.map((m) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              type: "button",
              title: m.hint,
              disabled: !m.enabled,
              onClick: () => m.enabled && setMode(m.id),
              className: cn(
                "flex-1 rounded-lg py-1.5 text-center text-xs font-medium transition-colors",
                mode === m.id && m.enabled ? "bg-blue-600 text-white" : "border border-slate-200 dark:border-slate-600 text-slate-400 dark:text-slate-300",
                !m.enabled && "cursor-not-allowed opacity-60"
              ),
              children: m.label
            },
            m.id
          )) }),
          mode === "composer" && registration && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border-b border-emerald-100 bg-emerald-50 px-3 py-1.5 text-[10px] text-emerald-800", children: [
            "📋 ",
            registration.title,
            " · RUC ",
            registration.ruc
          ] }),
          mode === "debug" && registration && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border-b border-orange-100 bg-orange-50 px-3 py-1.5 text-[10px] text-orange-900", children: [
            "🔍 Revisando ",
            registration.title,
            " · RUC ",
            registration.ruc
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "border-b border-slate-100 dark:border-slate-700 px-3 py-2 text-xs text-slate-500 dark:text-slate-400", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { htmlFor: "ai-thinking", className: "flex items-center justify-between gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
              "Pensamiento: ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { className: "text-slate-700 dark:text-slate-200", children: [
                Math.min(thinkingSeconds, maxThink),
                "s"
              ] }),
              mode === "composer" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-slate-400", children: " (1–45)" }),
              mode === "debug" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-slate-400", children: " (1–30)" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                id: "ai-thinking",
                type: "range",
                min: 1,
                max: maxThink,
                step: 1,
                value: Math.min(thinkingSeconds, maxThink),
                onChange: (e) => setThinkingSeconds(Number(e.target.value)),
                className: "w-32 accent-blue-600"
              }
            )
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 space-y-3 overflow-y-auto bg-slate-50 dark:bg-slate-900 p-3", children: [
            messages.map((m, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                className: cn(
                  "flex flex-col max-w-[92%]",
                  m.role === "user" ? "ml-auto items-end" : "items-start"
                ),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs(
                    "div",
                    {
                      className: cn(
                        "rounded-xl px-3 py-2 text-sm leading-relaxed whitespace-pre-wrap font-mono text-[12px]",
                        m.role === "user" ? "bg-blue-600 text-white rounded-br-sm font-sans text-sm" : "border border-slate-200 dark:border-slate-600 bg-white dark:bg-slate-800 text-slate-800 dark:text-slate-100 rounded-bl-sm",
                        m.streaming && m.streamKind === "debug" && "border-orange-300 dark:border-orange-500 ring-1 ring-orange-200 dark:ring-orange-700",
                        m.streaming && m.streamKind !== "debug" && "border-blue-300 dark:border-blue-500 ring-1 ring-blue-200 dark:ring-blue-700"
                      ),
                      children: [
                        m.streaming && m.streamKind === "debug" && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mb-1 flex items-center gap-1 font-sans text-[10px] text-orange-700", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(Bug, { className: "size-3 animate-pulse" }),
                          "Debug en vivo"
                        ] }),
                        m.streaming && m.streamKind !== "debug" && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "mb-1 flex items-center gap-1 font-sans text-[10px] text-blue-600", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "size-3 animate-pulse" }),
                          "Composer en vivo"
                        ] }),
                        m.content
                      ]
                    }
                  ),
                  m.toolsUsed && m.toolsUsed.length > 0 && !m.streaming && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "mt-0.5 font-sans text-[10px] text-slate-400 dark:text-slate-500", children: m.toolsUsed.join(", ") })
                ]
              },
              i
            )),
            loading && mode === "ask" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-sm italic text-slate-400 dark:text-slate-400", children: [
              "Analizando (~",
              Math.min(thinkingSeconds, maxThink),
              "s)…"
            ] }),
            loading && (mode === "composer" || mode === "debug") && /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                className: cn(
                  "rounded-lg border px-3 py-2 text-xs",
                  mode === "debug" ? "border-orange-200 bg-orange-50 text-orange-900" : "border-blue-200 bg-blue-50 text-blue-800"
                ),
                children: [
                  mode === "debug" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Bug, { className: "mb-1 inline size-3 animate-pulse" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "mb-1 inline size-3 animate-pulse" }),
                  " ",
                  loadingSteps(mode)[loadingStep]
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: bottomRef })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit: handleSubmit, className: "flex gap-2 border-t border-slate-100 dark:border-slate-700 bg-white dark:bg-slate-900 p-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                value: input,
                onChange: (e) => setInput(e.target.value),
                placeholder: mode === "composer" ? "Ej: Rellena la ficha con datos de la Clave SOL…" : mode === "debug" ? "Ej: Revisa los datos que rellenaste…" : "Ej: ¿Cuántos registros hay en plan_contable_pcge?",
                disabled: loading,
                className: "flex-1 rounded-xl border border-slate-200 dark:border-slate-600 bg-white dark:bg-slate-800 text-slate-900 dark:text-slate-100 placeholder-slate-400 dark:placeholder-slate-500 px-3 py-2 text-sm outline-none focus:border-blue-500 focus:ring-1 focus:ring-blue-500"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { type: "submit", size: "sm", disabled: loading || !input.trim(), children: "Enviar" })
          ] })
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        type: "button",
        onClick: () => setOpen((v) => !v),
        className: "fixed right-5 bottom-5 z-50 flex size-14 items-center justify-center rounded-full bg-blue-600 text-sm font-bold text-white shadow-lg shadow-blue-600/40 transition-transform hover:scale-105 active:scale-95",
        "aria-label": open ? "Cerrar CONTAM AI" : "Abrir CONTAM AI",
        children: open ? /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "size-5" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(MessageCircle, { className: "size-6" })
      }
    )
  ] });
}
function useStateMachine(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var SCROLL_AREA_NAME = "ScrollArea";
var [createScrollAreaContext] = createContextScope(SCROLL_AREA_NAME);
var [ScrollAreaProvider, useScrollAreaContext] = createScrollAreaContext(SCROLL_AREA_NAME);
var ScrollArea$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeScrollArea,
      type = "hover",
      dir,
      scrollHideDelay = 600,
      ...scrollAreaProps
    } = props;
    const [scrollArea, setScrollArea] = reactExports.useState(null);
    const [viewport, setViewport] = reactExports.useState(null);
    const [content, setContent] = reactExports.useState(null);
    const [scrollbarX, setScrollbarX] = reactExports.useState(null);
    const [scrollbarY, setScrollbarY] = reactExports.useState(null);
    const [cornerWidth, setCornerWidth] = reactExports.useState(0);
    const [cornerHeight, setCornerHeight] = reactExports.useState(0);
    const [scrollbarXEnabled, setScrollbarXEnabled] = reactExports.useState(false);
    const [scrollbarYEnabled, setScrollbarYEnabled] = reactExports.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setScrollArea(node));
    const direction = useDirection(dir);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaProvider,
      {
        scope: __scopeScrollArea,
        type,
        dir: direction,
        scrollHideDelay,
        scrollArea,
        viewport,
        onViewportChange: setViewport,
        content,
        onContentChange: setContent,
        scrollbarX,
        onScrollbarXChange: setScrollbarX,
        scrollbarXEnabled,
        onScrollbarXEnabledChange: setScrollbarXEnabled,
        scrollbarY,
        onScrollbarYChange: setScrollbarY,
        scrollbarYEnabled,
        onScrollbarYEnabledChange: setScrollbarYEnabled,
        onCornerWidthChange: setCornerWidth,
        onCornerHeightChange: setCornerHeight,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive.div,
          {
            dir: direction,
            ...scrollAreaProps,
            ref: composedRefs,
            style: {
              position: "relative",
              // Pass corner sizes as CSS vars to reduce re-renders of context consumers
              ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
              ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
              ...props.style
            }
          }
        )
      }
    );
  }
);
ScrollArea$1.displayName = SCROLL_AREA_NAME;
var VIEWPORT_NAME = "ScrollAreaViewport";
var ScrollAreaViewport = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, children, nonce, ...viewportProps } = props;
    const context = useScrollAreaContext(VIEWPORT_NAME, __scopeScrollArea);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref, context.onViewportChange);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "style",
        {
          dangerouslySetInnerHTML: {
            __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
          },
          nonce
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          "data-radix-scroll-area-viewport": "",
          ...viewportProps,
          ref: composedRefs,
          style: {
            /**
             * We don't support `visible` because the intention is to have at least one scrollbar
             * if this component is used and `visible` will behave like `auto` in that case
             * https://developer.mozilla.org/en-US/docs/Web/CSS/overflow#description
             *
             * We don't handle `auto` because the intention is for the native implementation
             * to be hidden if using this component. We just want to ensure the node is scrollable
             * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
             * the browser from having to work out whether to render native scrollbars or not,
             * we tell it to with the intention of hiding them in CSS.
             */
            overflowX: context.scrollbarXEnabled ? "scroll" : "hidden",
            overflowY: context.scrollbarYEnabled ? "scroll" : "hidden",
            ...props.style
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: context.onContentChange, style: { minWidth: "100%", display: "table" }, children })
        }
      )
    ] });
  }
);
ScrollAreaViewport.displayName = VIEWPORT_NAME;
var SCROLLBAR_NAME = "ScrollAreaScrollbar";
var ScrollAreaScrollbar = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context;
    const isHorizontal = props.orientation === "horizontal";
    reactExports.useEffect(() => {
      isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
      return () => {
        isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
      };
    }, [isHorizontal, onScrollbarXEnabledChange, onScrollbarYEnabledChange]);
    return context.type === "hover" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarHover, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "scroll" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarScroll, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "auto" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarAuto, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "always" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarVisible, { ...scrollbarProps, ref: forwardedRef }) : null;
  }
);
ScrollAreaScrollbar.displayName = SCROLLBAR_NAME;
var ScrollAreaScrollbarHover = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [visible, setVisible] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const scrollArea = context.scrollArea;
    let hideTimer = 0;
    if (scrollArea) {
      const handlePointerEnter = () => {
        window.clearTimeout(hideTimer);
        setVisible(true);
      };
      const handlePointerLeave = () => {
        hideTimer = window.setTimeout(() => setVisible(false), context.scrollHideDelay);
      };
      scrollArea.addEventListener("pointerenter", handlePointerEnter);
      scrollArea.addEventListener("pointerleave", handlePointerLeave);
      return () => {
        window.clearTimeout(hideTimer);
        scrollArea.removeEventListener("pointerenter", handlePointerEnter);
        scrollArea.removeEventListener("pointerleave", handlePointerLeave);
      };
    }
  }, [context.scrollArea, context.scrollHideDelay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarAuto,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarScroll = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const isHorizontal = props.orientation === "horizontal";
  const debounceScrollEnd = useDebounceCallback(() => send("SCROLL_END"), 100);
  const [state, send] = useStateMachine("hidden", {
    hidden: {
      SCROLL: "scrolling"
    },
    scrolling: {
      SCROLL_END: "idle",
      POINTER_ENTER: "interacting"
    },
    interacting: {
      SCROLL: "interacting",
      POINTER_LEAVE: "idle"
    },
    idle: {
      HIDE: "hidden",
      SCROLL: "scrolling",
      POINTER_ENTER: "interacting"
    }
  });
  reactExports.useEffect(() => {
    if (state === "idle") {
      const hideTimer = window.setTimeout(() => send("HIDE"), context.scrollHideDelay);
      return () => window.clearTimeout(hideTimer);
    }
  }, [state, context.scrollHideDelay, send]);
  reactExports.useEffect(() => {
    const viewport = context.viewport;
    const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
    if (viewport) {
      let prevScrollPos = viewport[scrollDirection];
      const handleScroll = () => {
        const scrollPos = viewport[scrollDirection];
        const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
        if (hasScrollInDirectionChanged) {
          send("SCROLL");
          debounceScrollEnd();
        }
        prevScrollPos = scrollPos;
      };
      viewport.addEventListener("scroll", handleScroll);
      return () => viewport.removeEventListener("scroll", handleScroll);
    }
  }, [context.viewport, isHorizontal, send, debounceScrollEnd]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || state !== "hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": state === "hidden" ? "hidden" : "visible",
      ...scrollbarProps,
      ref: forwardedRef,
      onPointerEnter: composeEventHandlers(props.onPointerEnter, () => send("POINTER_ENTER")),
      onPointerLeave: composeEventHandlers(props.onPointerLeave, () => send("POINTER_LEAVE"))
    }
  ) });
});
var ScrollAreaScrollbarAuto = reactExports.forwardRef((props, forwardedRef) => {
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const { forceMount, ...scrollbarProps } = props;
  const [visible, setVisible] = reactExports.useState(false);
  const isHorizontal = props.orientation === "horizontal";
  const handleResize = useDebounceCallback(() => {
    if (context.viewport) {
      const isOverflowX = context.viewport.offsetWidth < context.viewport.scrollWidth;
      const isOverflowY = context.viewport.offsetHeight < context.viewport.scrollHeight;
      setVisible(isHorizontal ? isOverflowX : isOverflowY);
    }
  }, 10);
  useResizeObserver(context.viewport, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarVisible = reactExports.forwardRef((props, forwardedRef) => {
  const { orientation = "vertical", ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const thumbRef = reactExports.useRef(null);
  const pointerOffsetRef = reactExports.useRef(0);
  const [sizes, setSizes] = reactExports.useState({
    content: 0,
    viewport: 0,
    scrollbar: { size: 0, paddingStart: 0, paddingEnd: 0 }
  });
  const thumbRatio = getThumbRatio(sizes.viewport, sizes.content);
  const commonProps = {
    ...scrollbarProps,
    sizes,
    onSizesChange: setSizes,
    hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
    onThumbChange: (thumb) => thumbRef.current = thumb,
    onThumbPointerUp: () => pointerOffsetRef.current = 0,
    onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
  };
  function getScrollPosition(pointerPos, dir) {
    return getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes, dir);
  }
  if (orientation === "horizontal") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarX,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollLeft;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes, context.dir);
            thumbRef.current.style.transform = `translate3d(${offset}px, 0, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollLeft = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) {
            context.viewport.scrollLeft = getScrollPosition(pointerPos, context.dir);
          }
        }
      }
    );
  }
  if (orientation === "vertical") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarY,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollTop;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes);
            thumbRef.current.style.transform = `translate3d(0, ${offset}px, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollTop = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) context.viewport.scrollTop = getScrollPosition(pointerPos);
        }
      }
    );
  }
  return null;
});
var ScrollAreaScrollbarX = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarXChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "horizontal",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        bottom: 0,
        left: context.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
        right: context.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
        ["--radix-scroll-area-thumb-width"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollLeft + event.deltaX;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollWidth,
            viewport: context.viewport.offsetWidth,
            scrollbar: {
              size: ref.current.clientWidth,
              paddingStart: toInt(computedStyle.paddingLeft),
              paddingEnd: toInt(computedStyle.paddingRight)
            }
          });
        }
      }
    }
  );
});
var ScrollAreaScrollbarY = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarYChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "vertical",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        top: 0,
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: "var(--radix-scroll-area-corner-height)",
        ["--radix-scroll-area-thumb-height"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollTop + event.deltaY;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollHeight,
            viewport: context.viewport.offsetHeight,
            scrollbar: {
              size: ref.current.clientHeight,
              paddingStart: toInt(computedStyle.paddingTop),
              paddingEnd: toInt(computedStyle.paddingBottom)
            }
          });
        }
      }
    }
  );
});
var [ScrollbarProvider, useScrollbarContext] = createScrollAreaContext(SCROLLBAR_NAME);
var ScrollAreaScrollbarImpl = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeScrollArea,
    sizes,
    hasThumb,
    onThumbChange,
    onThumbPointerUp,
    onThumbPointerDown,
    onThumbPositionChange,
    onDragScroll,
    onWheelScroll,
    onResize,
    ...scrollbarProps
  } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, __scopeScrollArea);
  const [scrollbar, setScrollbar] = reactExports.useState(null);
  const composeRefs = useComposedRefs(forwardedRef, (node) => setScrollbar(node));
  const rectRef = reactExports.useRef(null);
  const prevWebkitUserSelectRef = reactExports.useRef("");
  const viewport = context.viewport;
  const maxScrollPos = sizes.content - sizes.viewport;
  const handleWheelScroll = useCallbackRef(onWheelScroll);
  const handleThumbPositionChange = useCallbackRef(onThumbPositionChange);
  const handleResize = useDebounceCallback(onResize, 10);
  function handleDragScroll(event) {
    if (rectRef.current) {
      const x = event.clientX - rectRef.current.left;
      const y = event.clientY - rectRef.current.top;
      onDragScroll({ x, y });
    }
  }
  reactExports.useEffect(() => {
    const handleWheel = (event) => {
      const element = event.target;
      const isScrollbarWheel = scrollbar?.contains(element);
      if (isScrollbarWheel) handleWheelScroll(event, maxScrollPos);
    };
    document.addEventListener("wheel", handleWheel, { passive: false });
    return () => document.removeEventListener("wheel", handleWheel, { passive: false });
  }, [viewport, scrollbar, maxScrollPos, handleWheelScroll]);
  reactExports.useEffect(handleThumbPositionChange, [sizes, handleThumbPositionChange]);
  useResizeObserver(scrollbar, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollbarProvider,
    {
      scope: __scopeScrollArea,
      scrollbar,
      hasThumb,
      onThumbChange: useCallbackRef(onThumbChange),
      onThumbPointerUp: useCallbackRef(onThumbPointerUp),
      onThumbPositionChange: handleThumbPositionChange,
      onThumbPointerDown: useCallbackRef(onThumbPointerDown),
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          ...scrollbarProps,
          ref: composeRefs,
          style: { position: "absolute", ...scrollbarProps.style },
          onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
            const mainPointer = 0;
            if (event.button === mainPointer) {
              const element = event.target;
              element.setPointerCapture(event.pointerId);
              rectRef.current = scrollbar.getBoundingClientRect();
              prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
              document.body.style.webkitUserSelect = "none";
              if (context.viewport) context.viewport.style.scrollBehavior = "auto";
              handleDragScroll(event);
            }
          }),
          onPointerMove: composeEventHandlers(props.onPointerMove, handleDragScroll),
          onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
            const element = event.target;
            if (element.hasPointerCapture(event.pointerId)) {
              element.releasePointerCapture(event.pointerId);
            }
            document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
            if (context.viewport) context.viewport.style.scrollBehavior = "";
            rectRef.current = null;
          })
        }
      )
    }
  );
});
var THUMB_NAME = "ScrollAreaThumb";
var ScrollAreaThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...thumbProps } = props;
    const scrollbarContext = useScrollbarContext(THUMB_NAME, props.__scopeScrollArea);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || scrollbarContext.hasThumb, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaThumbImpl, { ref: forwardedRef, ...thumbProps }) });
  }
);
var ScrollAreaThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, style, ...thumbProps } = props;
    const scrollAreaContext = useScrollAreaContext(THUMB_NAME, __scopeScrollArea);
    const scrollbarContext = useScrollbarContext(THUMB_NAME, __scopeScrollArea);
    const { onThumbPositionChange } = scrollbarContext;
    const composedRef = useComposedRefs(
      forwardedRef,
      (node) => scrollbarContext.onThumbChange(node)
    );
    const removeUnlinkedScrollListenerRef = reactExports.useRef(void 0);
    const debounceScrollEnd = useDebounceCallback(() => {
      if (removeUnlinkedScrollListenerRef.current) {
        removeUnlinkedScrollListenerRef.current();
        removeUnlinkedScrollListenerRef.current = void 0;
      }
    }, 100);
    reactExports.useEffect(() => {
      const viewport = scrollAreaContext.viewport;
      if (viewport) {
        const handleScroll = () => {
          debounceScrollEnd();
          if (!removeUnlinkedScrollListenerRef.current) {
            const listener = addUnlinkedScrollListener(viewport, onThumbPositionChange);
            removeUnlinkedScrollListenerRef.current = listener;
            onThumbPositionChange();
          }
        };
        onThumbPositionChange();
        viewport.addEventListener("scroll", handleScroll);
        return () => viewport.removeEventListener("scroll", handleScroll);
      }
    }, [scrollAreaContext.viewport, debounceScrollEnd, onThumbPositionChange]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        "data-state": scrollbarContext.hasThumb ? "visible" : "hidden",
        ...thumbProps,
        ref: composedRef,
        style: {
          width: "var(--radix-scroll-area-thumb-width)",
          height: "var(--radix-scroll-area-thumb-height)",
          ...style
        },
        onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, (event) => {
          const thumb = event.target;
          const thumbRect = thumb.getBoundingClientRect();
          const x = event.clientX - thumbRect.left;
          const y = event.clientY - thumbRect.top;
          scrollbarContext.onThumbPointerDown({ x, y });
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, scrollbarContext.onThumbPointerUp)
      }
    );
  }
);
ScrollAreaThumb.displayName = THUMB_NAME;
var CORNER_NAME = "ScrollAreaCorner";
var ScrollAreaCorner = reactExports.forwardRef(
  (props, forwardedRef) => {
    const context = useScrollAreaContext(CORNER_NAME, props.__scopeScrollArea);
    const hasBothScrollbarsVisible = Boolean(context.scrollbarX && context.scrollbarY);
    const hasCorner = context.type !== "scroll" && hasBothScrollbarsVisible;
    return hasCorner ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaCornerImpl, { ...props, ref: forwardedRef }) : null;
  }
);
ScrollAreaCorner.displayName = CORNER_NAME;
var ScrollAreaCornerImpl = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeScrollArea, ...cornerProps } = props;
  const context = useScrollAreaContext(CORNER_NAME, __scopeScrollArea);
  const [width, setWidth] = reactExports.useState(0);
  const [height, setHeight] = reactExports.useState(0);
  const hasSize = Boolean(width && height);
  useResizeObserver(context.scrollbarX, () => {
    const height2 = context.scrollbarX?.offsetHeight || 0;
    context.onCornerHeightChange(height2);
    setHeight(height2);
  });
  useResizeObserver(context.scrollbarY, () => {
    const width2 = context.scrollbarY?.offsetWidth || 0;
    context.onCornerWidthChange(width2);
    setWidth(width2);
  });
  return hasSize ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.div,
    {
      ...cornerProps,
      ref: forwardedRef,
      style: {
        width,
        height,
        position: "absolute",
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: 0,
        ...props.style
      }
    }
  ) : null;
});
function toInt(value) {
  return value ? parseInt(value, 10) : 0;
}
function getThumbRatio(viewportSize, contentSize) {
  const ratio = viewportSize / contentSize;
  return isNaN(ratio) ? 0 : ratio;
}
function getThumbSize(sizes) {
  const ratio = getThumbRatio(sizes.viewport, sizes.content);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
  return Math.max(thumbSize, 18);
}
function getScrollPositionFromPointer(pointerPos, pointerOffset, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const thumbCenter = thumbSizePx / 2;
  const offset = pointerOffset || thumbCenter;
  const thumbOffsetFromEnd = thumbSizePx - offset;
  const minPointerPos = sizes.scrollbar.paddingStart + offset;
  const maxPointerPos = sizes.scrollbar.size - sizes.scrollbar.paddingEnd - thumbOffsetFromEnd;
  const maxScrollPos = sizes.content - sizes.viewport;
  const scrollRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const interpolate = linearScale([minPointerPos, maxPointerPos], scrollRange);
  return interpolate(pointerPos);
}
function getThumbOffsetFromScroll(scrollPos, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const scrollbar = sizes.scrollbar.size - scrollbarPadding;
  const maxScrollPos = sizes.content - sizes.viewport;
  const maxThumbPos = scrollbar - thumbSizePx;
  const scrollClampRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const scrollWithoutMomentum = clamp(scrollPos, scrollClampRange);
  const interpolate = linearScale([0, maxScrollPos], [0, maxThumbPos]);
  return interpolate(scrollWithoutMomentum);
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
  return scrollPos > 0 && scrollPos < maxScrollPos;
}
var addUnlinkedScrollListener = (node, handler = () => {
}) => {
  let prevPosition = { left: node.scrollLeft, top: node.scrollTop };
  let rAF = 0;
  (function loop() {
    const position = { left: node.scrollLeft, top: node.scrollTop };
    const isHorizontalScroll = prevPosition.left !== position.left;
    const isVerticalScroll = prevPosition.top !== position.top;
    if (isHorizontalScroll || isVerticalScroll) handler();
    prevPosition = position;
    rAF = window.requestAnimationFrame(loop);
  })();
  return () => window.cancelAnimationFrame(rAF);
};
function useDebounceCallback(callback, delay) {
  const handleCallback = useCallbackRef(callback);
  const debounceTimerRef = reactExports.useRef(0);
  reactExports.useEffect(() => () => window.clearTimeout(debounceTimerRef.current), []);
  return reactExports.useCallback(() => {
    window.clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = window.setTimeout(handleCallback, delay);
  }, [handleCallback, delay]);
}
function useResizeObserver(element, onResize) {
  const handleResize = useCallbackRef(onResize);
  useLayoutEffect2(() => {
    let rAF = 0;
    if (element) {
      const resizeObserver = new ResizeObserver(() => {
        cancelAnimationFrame(rAF);
        rAF = window.requestAnimationFrame(handleResize);
      });
      resizeObserver.observe(element);
      return () => {
        window.cancelAnimationFrame(rAF);
        resizeObserver.unobserve(element);
      };
    }
  }, [element, handleResize]);
}
var Root = ScrollArea$1;
var Viewport = ScrollAreaViewport;
var Corner = ScrollAreaCorner;
const ScrollArea = reactExports.forwardRef(({ className, children, ...props }, ref) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
  Root,
  {
    ref,
    className: cn("relative overflow-hidden", className),
    ...props,
    children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Viewport, { className: "h-full w-full rounded-[inherit]", children }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollBar, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Corner, {})
    ]
  }
));
ScrollArea.displayName = Root.displayName;
const ScrollBar = reactExports.forwardRef(({ className, orientation = "vertical", ...props }, ref) => /* @__PURE__ */ jsxRuntimeExports.jsx(
  ScrollAreaScrollbar,
  {
    ref,
    orientation,
    className: cn(
      "flex touch-none select-none transition-colors",
      orientation === "vertical" && "h-full w-2.5 border-l border-l-transparent p-[1px]",
      orientation === "horizontal" && "h-2.5 flex-col border-t border-t-transparent p-[1px]",
      className
    ),
    ...props,
    children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaThumb, { className: "relative flex-1 rounded-full bg-border" })
  }
));
ScrollBar.displayName = ScrollAreaScrollbar.displayName;
const ICON_MAP = {
  RefreshCw,
  XCircle: CircleX,
  ArrowUpCircle: CircleArrowUp,
  Save,
  UserPlus,
  ShieldAlert,
  AlertTriangle: TriangleAlert,
  Wrench,
  Bell
};
function formatRelative(iso) {
  const diff = Date.now() - new Date(iso).getTime();
  const mins = Math.floor(diff / 6e4);
  if (mins < 1) return "Ahora";
  if (mins < 60) return `Hace ${mins} min`;
  const hrs = Math.floor(mins / 60);
  if (hrs < 24) return `Hace ${hrs} h`;
  return new Date(iso).toLocaleDateString("es-PE");
}
function SystemNotifRow({
  n,
  onRead
}) {
  const Icon = ICON_MAP[n.icon] ?? Bell;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "button",
    {
      type: "button",
      onClick: () => void onRead(n.id),
      className: cn(
        "w-full text-left rounded-xl p-3 transition-colors border border-transparent hover:border-[#1A2740]/80",
        n.read ? "opacity-70" : "bg-white/[0.03]"
      ),
      children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "div",
          {
            className: "size-8 rounded-lg grid place-items-center shrink-0",
            style: { backgroundColor: `${n.color}22`, color: n.color },
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "size-4" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-[#E8EDF5] truncate", children: n.title }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-[#8899B4] line-clamp-2 mt-0.5", children: n.message }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-[#8899B4]/80 mt-1", children: formatRelative(n.createdAt) })
        ] })
      ] })
    }
  );
}
function SystemNotificationsDropdownPremium() {
  const [open, setOpen] = reactExports.useState(false);
  const badge = useAlertBadge("notifications");
  const { alertState, markSystemNotificationRead, markAllSystemNotificationsRead } = useAlertSystem();
  const items = alertState.systemNotifications.slice(0, 12);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(DropdownMenu, { open, onOpenChange: setOpen, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(DropdownMenuTrigger, { asChild: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
      Button,
      {
        variant: "ghost",
        size: "icon",
        className: cn(
          "relative rounded-xl text-muted-foreground hover:text-foreground",
          open && "bg-white/[0.06]"
        ),
        "aria-label": badge.tooltip,
        title: badge.tooltip,
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Bell, { className: "size-5" }),
          badge.showBadge && /* @__PURE__ */ jsxRuntimeExports.jsx(
            "span",
            {
              className: cn(
                "absolute -top-1 -right-1 flex items-center justify-center min-w-[18px] h-[18px] px-1 rounded-full text-[10px] font-semibold animate-fade-badge"
              ),
              style: {
                backgroundColor: "rgba(0, 212, 255, 0.2)",
                color: "#00D4FF"
              },
              children: badge.count > 99 ? "99+" : badge.count
            }
          )
        ]
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      DropdownMenuContent,
      {
        align: "end",
        sideOffset: 8,
        className: "w-[400px] p-0 border-[#1A2740]/60 bg-[#0D1525]/95 backdrop-blur-xl shadow-2xl",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between px-4 py-3 border-b border-[#1A2740]/60", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Bell, { className: "size-4 text-[#00D4FF]" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-semibold text-sm text-[#E8EDF5]", children: "Notificaciones del sistema" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "icon", className: "size-7", asChild: true, title: "Preferencias", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Link, { to: "/mi-cuenta", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Settings, { className: "size-3.5" }) }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "ghost",
                  size: "icon",
                  className: "size-7",
                  onClick: () => void markAllSystemNotificationsRead(),
                  title: "Marcar todas como leídas",
                  disabled: badge.count === 0,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "size-3.5" })
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "max-h-[360px] px-3 py-2", children: items.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-[#8899B4] text-center py-8", children: "Sin notificaciones del sistema" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-1", children: items.map((n) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            SystemNotifRow,
            {
              n,
              onRead: (id) => void markSystemNotificationRead(id)
            },
            n.id
          )) }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "border-t border-[#1A2740]/60 px-4 py-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "sm", className: "justify-start text-xs text-[#00D4FF] w-full", asChild: true, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Link, { to: "/notificaciones", onClick: () => setOpen(false), children: "Ver historial completo →" }) }) })
        ]
      }
    )
  ] });
}
function SidebarAlertBadge({
  type,
  className
}) {
  const badge = useAlertBadge(type);
  if (!badge.showBadge) return null;
  const isTasks = type === "tasks";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "span",
    {
      className: cn(
        "absolute top-1.5 right-2 flex items-center justify-center rounded-full font-bold text-white",
        isTasks ? "min-w-5 h-5 text-[10px]" : "min-w-[18px] h-[18px] text-[9px] font-semibold",
        isTasks && badge.isPulsing && "animate-pulse-red",
        className
      ),
      style: isTasks ? { backgroundColor: badge.color } : { backgroundColor: "rgba(0, 212, 255, 0.2)", color: "#00D4FF" },
      title: badge.tooltip,
      children: badge.count > 99 ? "99+" : badge.count
    }
  );
}
function UrgentTasksTopbarIndicator() {
  const { alertState, setFabDrawerOpen } = useAlertSystem();
  const { overdueTaskCount, todayTaskCount } = alertState;
  if (overdueTaskCount === 0 && todayTaskCount === 0) return null;
  const parts = [];
  if (overdueTaskCount) parts.push(`${overdueTaskCount} vencida${overdueTaskCount > 1 ? "s" : ""}`);
  if (todayTaskCount) parts.push(`${todayTaskCount} para hoy`);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "button",
    {
      type: "button",
      onClick: () => setFabDrawerOpen(true),
      className: "hidden md:flex items-center gap-1.5 rounded-xl border border-[#FF6B00]/30 bg-[#FF6B00]/10 px-3 py-1.5 text-xs text-[#FF6B00] hover:bg-[#FF6B00]/20 transition-colors",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "size-3.5" }),
        parts.join(" | ")
      ]
    }
  );
}
function AppTopBar({ title }) {
  const { darkMode, expandedMode, toggleDarkMode } = useUiPreferences();
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "sticky top-0 z-30 flex items-center justify-between gap-4 border-b border-border/60 glass-surface shadow-premium-subtle px-6 py-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 min-w-0", children: [
      expandedMode && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-xs text-muted-foreground", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Maximize2, { className: "size-3.5 text-primary" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "hidden sm:inline", children: "Modo Expandido — pantalla completa activa" })
      ] }),
      title && !expandedMode && /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-medium text-muted-foreground truncate", children: title })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 shrink-0", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hidden sm:flex items-center gap-2 rounded-xl glass-surface px-3 py-1.5 shadow-premium-subtle", children: [
        darkMode ? /* @__PURE__ */ jsxRuntimeExports.jsx(Moon, { className: "size-3.5 text-primary" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Sun, { className: "size-3.5 text-amber-500" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: "dark-mode-toggle", className: "text-xs cursor-pointer select-none", children: "Modo Nocturno" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Switch,
          {
            id: "dark-mode-toggle",
            checked: darkMode,
            onCheckedChange: (checked) => {
              if (checked !== darkMode) toggleDarkMode();
            }
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Button,
        {
          variant: "ghost",
          size: "icon",
          className: cn("sm:hidden rounded-xl", darkMode ? "text-primary" : "text-amber-500"),
          onClick: toggleDarkMode,
          "aria-label": darkMode ? "Desactivar modo nocturno" : "Activar modo nocturno",
          children: darkMode ? /* @__PURE__ */ jsxRuntimeExports.jsx(Moon, { className: "size-5" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Sun, { className: "size-5" })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(UrgentTasksTopbarIndicator, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx(SystemNotificationsDropdownPremium, {})
    ] })
  ] });
}
function ExpandedModeHint() {
  const { expandedMode, toggleExpandedMode } = useUiPreferences();
  if (!expandedMode) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Button,
    {
      variant: "outline",
      size: "sm",
      onClick: toggleExpandedMode,
      className: "fixed bottom-20 left-6 z-40 shadow-lg gap-2 rounded-xl bg-background/90 backdrop-blur",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Minimize2, { className: "size-4" }),
        "Restaurar navegación"
      ]
    }
  );
}
const FAB_POS_KEY = "contam-fab-position";
const CORNER_CLASS = {
  "bottom-right": "right-6 bottom-24",
  "bottom-left": "left-6 bottom-24",
  "top-right": "right-6 top-24",
  "top-left": "left-6 top-24"
};
function urgencyLabel(task) {
  if (task.urgency === "OVERDUE") return "VENCIDA";
  if (task.urgency === "TODAY") return "VENCE HOY";
  if (task.urgency === "TOMORROW") return "VENCE MAÑANA";
  return "PRÓXIMA";
}
function TaskCard({
  task,
  onComplete,
  onSnooze
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "rounded-xl border border-white/10 bg-[#0D1525]/80 p-3 space-y-2",
      style: { borderLeftColor: task.badgeColor, borderLeftWidth: 3 },
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-start justify-between gap-2", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-[10px] font-bold tracking-wide", style: { color: task.badgeColor }, children: [
            task.priority,
            " · ",
            urgencyLabel(task)
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-[#E8EDF5] mt-0.5", children: task.title }),
          task.rucName && /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-[#8899B4] mt-1", children: [
            task.rucName,
            task.ruc ? ` · ${task.ruc}` : ""
          ] }),
          task.dueDate && /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-[10px] text-[#8899B4] mt-1 flex items-center gap-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Clock, { className: "size-3" }),
            task.urgency === "OVERDUE" ? "Vencida" : "Vence",
            ": ",
            task.dueDate
          ] })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-wrap gap-1.5 pt-1", children: [
          task.metadata?.link && /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "sm", className: "h-7 text-xs text-[#00D4FF]", asChild: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: task.metadata.link, children: [
            "Ir al módulo",
            /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "size-3 ml-1" })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              variant: "ghost",
              size: "sm",
              className: "h-7 text-xs",
              onClick: () => onSnooze(task.id),
              children: "Posponer"
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              variant: "ghost",
              size: "sm",
              className: "h-7 text-xs text-emerald-400",
              onClick: () => onComplete(task.id),
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "size-3 mr-1" }),
                "Hecho"
              ]
            }
          )
        ] })
      ]
    }
  );
}
function FloatingAlertButtonPremium() {
  const {
    visible,
    badgeCount,
    badgeColor,
    isPulsing,
    isShaking,
    tooltip,
    urgentTasks,
    drawerOpen,
    openDrawer,
    closeDrawer
  } = useFAB();
  const { completeTask, snoozeTask, alertState } = useAlertSystem();
  const [corner, setCorner] = reactExports.useState("bottom-right");
  const [dragging, setDragging] = reactExports.useState(false);
  const dragRef = reactExports.useRef(null);
  const reducedMotion = typeof window !== "undefined" && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  reactExports.useEffect(() => {
    const saved = localStorage.getItem(FAB_POS_KEY);
    if (saved && CORNER_CLASS[saved]) setCorner(saved);
  }, []);
  const handlePointerDown = reactExports.useCallback((e) => {
    dragRef.current = { x: e.clientX, y: e.clientY };
    setDragging(false);
  }, []);
  const handlePointerUp = reactExports.useCallback(
    (e) => {
      if (!dragRef.current) return;
      const dx = Math.abs(e.clientX - dragRef.current.x);
      const dy = Math.abs(e.clientY - dragRef.current.y);
      dragRef.current = null;
      if (dx > 8 || dy > 8) {
        setDragging(true);
        const cx = e.clientX;
        const cy = e.clientY;
        const w = window.innerWidth;
        const h = window.innerHeight;
        let next = "bottom-right";
        if (cx < w / 2 && cy < h / 2) next = "top-left";
        else if (cx >= w / 2 && cy < h / 2) next = "top-right";
        else if (cx < w / 2) next = "bottom-left";
        setCorner(next);
        localStorage.setItem(FAB_POS_KEY, next);
        return;
      }
      openDrawer();
    },
    [openDrawer]
  );
  const handleDoubleClick = reactExports.useCallback(() => {
    const top = urgentTasks[0] ?? alertState.pendingTasks[0];
    if (top) void snoozeTask(top.id, 1);
  }, [urgentTasks, alertState.pendingTasks, snoozeTask]);
  const handleContextMenu = reactExports.useCallback(
    (e) => {
      e.preventDefault();
      const choice = window.prompt(
        "Alertas: 1=Ver todas 2=Posponer 1h 3=Silenciar hasta mañana 4=Configurar"
      );
      if (choice === "1") window.location.href = "/tareas";
      else if (choice === "2") {
        const top = urgentTasks[0];
        if (top) void snoozeTask(top.id, 1);
      } else if (choice === "3") {
        const top = urgentTasks[0];
        if (top) void snoozeTask(top.id, 24);
      } else if (choice === "4") window.location.href = "/mi-cuenta";
    },
    [urgentTasks, snoozeTask]
  );
  if (!visible) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "button",
      {
        type: "button",
        title: tooltip,
        "aria-label": tooltip,
        onPointerDown: handlePointerDown,
        onPointerUp: handlePointerUp,
        onDoubleClick: handleDoubleClick,
        onContextMenu: handleContextMenu,
        className: cn(
          "fixed z-[9999] flex flex-col items-center justify-center size-14 rounded-2xl border border-white/10 shadow-lg transition-transform hover:scale-105 fab-enter",
          CORNER_CLASS[corner],
          !reducedMotion && isPulsing && "animate-pulse-red",
          !reducedMotion && isShaking && !dragging && "animate-shake-alert"
        ),
        style: {
          backgroundColor: badgeColor,
          boxShadow: badgeColor.includes("FF") ? "0 4px 20px rgba(255, 0, 0, 0.35)" : "0 4px 16px rgba(0, 0, 0, 0.25)"
        },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(ClipboardList, { className: "size-6 text-white", strokeWidth: 1.75 }),
          badgeCount > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "absolute -top-1.5 -right-1.5 min-w-5 h-5 px-1 rounded-full bg-white text-[#FF0000] text-[11px] font-bold flex items-center justify-center", children: badgeCount > 99 ? "99+" : badgeCount })
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Sheet, { open: drawerOpen, onOpenChange: (o) => o ? openDrawer() : closeDrawer(), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
      SheetContent,
      {
        side: "right",
        className: "w-full sm:max-w-md border-[#1A2740]/60 bg-[#0D1525]/98 backdrop-blur-xl p-0",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(SheetHeader, { className: "px-4 py-4 border-b border-[#1A2740]/60", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(SheetTitle, { className: "flex items-center gap-2 text-[#E8EDF5]", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "size-4 text-[#FF6B00]" }),
            "Tareas urgentes (",
            urgentTasks.length || badgeCount,
            ")"
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "flex-1 max-h-[calc(100vh-10rem)] px-4 py-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-3", children: urgentTasks.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-[#8899B4] text-center py-8", children: "Sin tareas urgentes por ahora" }) : urgentTasks.map((t) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            TaskCard,
            {
              task: t,
              onComplete: (id) => void completeTask(id),
              onSnooze: (id) => void snoozeTask(id, 1)
            },
            t.id
          )) }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border-t border-[#1A2740]/60 px-4 py-3 space-y-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "sm", className: "w-full justify-start text-xs text-[#00D4FF]", asChild: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: "/tareas", onClick: closeDrawer, children: [
              "Ver todas las tareas (",
              alertState.pendingTaskCount,
              ")"
            ] }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "sm", className: "w-full justify-start text-xs", asChild: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: "/mi-cuenta", onClick: closeDrawer, children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Settings, { className: "size-3 mr-2" }),
              "Configurar alertas"
            ] }) })
          ] })
        ]
      }
    ) })
  ] });
}
function ModePill({
  active,
  onClick,
  children
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "button",
    {
      type: "button",
      onClick,
      className: cn(
        "flex flex-1 flex-col items-center gap-1 rounded-lg border px-2 py-2 text-xs transition-all duration-200",
        active ? "border-[var(--fin-premium-text,#C8A95A)] bg-[var(--fin-premium-bg)] text-[var(--fin-premium-text,#C8A95A)]" : "border-border/60 bg-transparent text-muted-foreground hover:bg-muted/40"
      ),
      children
    }
  );
}
function ColorBlindPreview({ mode }) {
  const colors = mode === "protanopia" ? ["#00B878", "#D4A000", "#4A90D9"] : mode === "deuteranopia" ? ["#0088CC", "#CC5500", "#4477CC"] : mode === "tritanopia" ? ["#00AA88", "#DD3355", "#44AAAA"] : ["var(--fin-gain-text)", "var(--fin-loss-text)", "var(--fin-security-text)"];
  return /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "flex gap-0.5 ml-1", children: colors.map((c) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "size-2 rounded-full border border-border/40", style: { background: c } }, c)) });
}
function AccessibilityLoupePremium() {
  const {
    config,
    resolvedMode,
    setMode,
    setColorBlindMode,
    setContrast,
    setFontScale,
    increaseFontScale,
    decreaseFontScale,
    resetFontScale,
    setReducedMotion,
    setDenseMode,
    setShowGridLines,
    setHighContrastFocus,
    resetTheme
  } = useAccessibility();
  const [open, setOpen] = reactExports.useState(false);
  const panelId = reactExports.useId();
  const toggleOpen = reactExports.useCallback(() => setOpen((v) => !v), []);
  reactExports.useEffect(() => {
    const handler = () => toggleOpen();
    window.addEventListener("contam:toggle-a11y-loupe", handler);
    return () => window.removeEventListener("contam:toggle-a11y-loupe", handler);
  }, [toggleOpen]);
  reactExports.useEffect(() => {
    if (!open) return;
    const onEsc = (e) => {
      if (e.key === "Escape") setOpen(false);
    };
    window.addEventListener("keydown", onEsc);
    return () => window.removeEventListener("keydown", onEsc);
  }, [open]);
  const sliderIndex = FONT_SCALE_STEPS.indexOf(config.fontScale);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
    config.fontScale !== 100 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "fixed bottom-6 left-20 z-[60] rounded-full border border-[#00D4FF]/40 bg-[#0D1525]/90 px-2.5 py-1 text-xs font-medium text-[#00D4FF] shadow-lg backdrop-blur-xl animate-in fade-in duration-200 dark:bg-[#0D1525]/90",
        "aria-live": "polite",
        children: [
          config.fontScale,
          "%"
        ]
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      Button,
      {
        type: "button",
        variant: "outline",
        size: "icon",
        "aria-expanded": open,
        "aria-controls": panelId,
        "aria-label": "Abrir asistente de accesibilidad",
        title: "Accesibilidad (Ctrl+Shift+A)",
        onClick: toggleOpen,
        className: cn(
          "fixed bottom-6 left-6 z-[60] size-12 rounded-full border shadow-lg transition-transform duration-200 hover:scale-110",
          resolvedMode === "dark" ? "border-white/10 bg-[#0D1525]/95 text-[#C8A95A] hover:shadow-[0_0_20px_rgba(200,169,90,0.25)]" : "border-black/10 bg-white/95 text-[#B8860B] hover:shadow-md"
        ),
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "size-5", strokeWidth: 2 })
      }
    ),
    open && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          type: "button",
          className: "fixed inset-0 z-[59] bg-black/40 backdrop-blur-[2px]",
          "aria-label": "Cerrar panel",
          onClick: () => setOpen(false)
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "div",
        {
          id: panelId,
          role: "dialog",
          "aria-modal": "true",
          "aria-labelledby": `${panelId}-title`,
          className: cn(
            "fixed bottom-24 left-6 z-[60] w-[min(420px,calc(100vw-3rem))] max-h-[min(80vh,640px)] overflow-y-auto rounded-2xl border p-4 shadow-2xl",
            "animate-in zoom-in-95 fade-in duration-200",
            resolvedMode === "dark" ? "border-white/[0.08] bg-[#0D1525]/95 text-[#E8EDF5] backdrop-blur-xl" : "border-black/[0.08] bg-white/95 text-[#1E293B] backdrop-blur-xl"
          ),
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-4 flex items-center justify-between gap-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "size-4 text-[#C8A95A]" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { id: `${panelId}-title`, className: "text-sm font-semibold tracking-wide", children: "ACCESIBILIDAD" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "icon", className: "size-8", onClick: () => setOpen(false), "aria-label": "Cerrar", children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "size-4" }) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "space-y-3 mb-5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-xs font-medium text-muted-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Eye, { className: "size-3.5" }),
                "TAMAÑO DE TEXTO"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", size: "icon", className: "size-8 shrink-0", onClick: decreaseFontScale, "aria-label": "Reducir texto", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Minus, { className: "size-3.5" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  Slider,
                  {
                    value: [sliderIndex >= 0 ? sliderIndex : 2],
                    min: 0,
                    max: FONT_SCALE_STEPS.length - 1,
                    step: 1,
                    onValueChange: ([v]) => setFontScale(FONT_SCALE_STEPS[v]),
                    className: "flex-1",
                    "aria-label": "Escala de texto"
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", size: "icon", className: "size-8 shrink-0", onClick: increaseFontScale, "aria-label": "Aumentar texto", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "size-3.5" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "w-12 text-right text-xs font-mono text-[#00D4FF]", children: [
                  config.fontScale,
                  "%"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-muted-foreground", children: "Atajos: Ctrl+Shift+ +/- / 0 para restablecer" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "space-y-2 mb-5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-medium text-muted-foreground", children: "MODO DE COLOR" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-2", children: [
                ["dark", Moon, "Oscuro"],
                ["light", Sun, "Claro"],
                ["system", Monitor, "Auto"]
              ].map(([mode, Icon, label]) => /* @__PURE__ */ jsxRuntimeExports.jsxs(ModePill, { active: config.mode === mode, onClick: () => setMode(mode), children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "size-4" }),
                label
              ] }, mode)) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "space-y-2 mb-5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-medium text-muted-foreground", children: "CONTRASTE" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-2", children: ["normal", "high", "maximum"].map((level) => /* @__PURE__ */ jsxRuntimeExports.jsx(ModePill, { active: config.contrastLevel === level, onClick: () => setContrast(level), children: level === "normal" ? "Normal" : level === "high" ? "Alto" : "Máximo" }, level)) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "space-y-2 mb-5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-medium text-muted-foreground", children: "DALTONISMO" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-2 gap-2", children: [
                ["none", "Ninguno"],
                ["protanopia", "Protanopía"],
                ["deuteranopia", "Deuteranopía"],
                ["tritanopia", "Tritanopía"]
              ].map(([mode, label]) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "button",
                {
                  type: "button",
                  title: mode !== "none" ? "Simulación de percepción cromática — verifique con su médico" : void 0,
                  onClick: () => setColorBlindMode(mode),
                  className: cn(
                    "flex items-center justify-between rounded-lg border px-2 py-2 text-xs transition-all duration-200",
                    config.colorBlindMode === mode ? "border-[var(--fin-premium-text,#C8A95A)] bg-[var(--fin-premium-bg)]" : "border-border/60 hover:bg-muted/40"
                  ),
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: label }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(ColorBlindPreview, { mode })
                  ]
                },
                mode
              )) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("section", { className: "space-y-3 mb-5 border-t border-border/40 pt-4", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-medium text-muted-foreground", children: "OPCIONES ADICIONALES" }),
              [
                [config.reducedMotion, setReducedMotion, "Reducir animaciones"],
                [config.denseMode, setDenseMode, "Modo compacto"],
                [config.showGridLines, setShowGridLines, "Líneas de cuadrícula en tablas"],
                [config.highContrastFocus, setHighContrastFocus, "Anillos de focus mejorados"]
              ].map(([checked, setter, label]) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: `a11y-${label}`, className: "text-xs cursor-pointer", children: label }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  Switch,
                  {
                    id: `a11y-${label}`,
                    checked,
                    onCheckedChange: setter
                  }
                )
              ] }, label))
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2 border-t border-border/40 pt-4", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "outline",
                  size: "sm",
                  className: "flex-1 text-xs",
                  onClick: () => {
                    resetTheme();
                    resetFontScale();
                  },
                  children: "Restaurar defaults"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", className: "flex-1 text-xs", onClick: () => setOpen(false), children: "Cerrar" })
            ] })
          ]
        }
      )
    ] })
  ] });
}
const nav = [{
  to: "/dashboard",
  label: "Dashboard",
  icon: LayoutDashboard
}, {
  to: "/contribuyentes",
  label: "Contribuyentes",
  icon: Building2
}, {
  to: "/ficha-ruc",
  label: "Ficha RUC",
  icon: FileText
}, {
  to: "/profiling",
  label: "Profiling RUC",
  icon: Scale
}, {
  to: "/importador",
  label: "Importador",
  icon: Upload
}, {
  to: "/compras-ventas",
  label: "Compras/Ventas",
  icon: Receipt
}, {
  to: "/contabilidad",
  label: "Contabilidad",
  icon: BookOpen
}, {
  to: "/libro-mayor",
  label: "Libro Mayor",
  icon: BookMarked
}, {
  to: "/workflow",
  label: "Flujo Contable",
  icon: GitBranch
}, {
  to: "/tesoreria",
  label: "Tesorería",
  icon: Wallet
}, {
  to: "/control-formal",
  label: "Control Formal",
  icon: ShieldAlert
}, {
  to: "/ai-copilot",
  label: "AI Copilot",
  icon: Sparkles
}, {
  to: "/pcge",
  label: "Plan de Cuentas",
  icon: Landmark
}, {
  to: "/tareas",
  label: "Tareas",
  icon: ClipboardList
}, {
  to: "/libro-caja",
  label: "Libro Caja",
  icon: Wallet
}, {
  to: "/sire-registros",
  label: "Registros SIRE",
  icon: FileSpreadsheet
}, {
  to: "/sire-sync",
  label: "Sync SIRE",
  icon: RefreshCw
}, {
  to: "/libro-diario",
  label: "Libro Diario",
  icon: BookOpen
}, {
  to: "/dashboard-estadisticas",
  label: "Estadísticas SIRE",
  icon: ChartColumn
}, {
  to: "/mi-cuenta",
  label: "Mi Cuenta",
  icon: CircleUser
}];
const adminNav = {
  to: "/admin/usuarios",
  label: "Admin RBAC"
};
const auditoriaNav = {
  to: "/admin/auditoria",
  label: "Auditoría"
};
const performanceNav = {
  to: "/admin/performance",
  label: "Rendimiento"
};
const configuracionNav = {
  to: "/admin/configuracion",
  label: "Configuración"
};
function AppLayout() {
  const {
    session,
    loading,
    error
  } = useSession();
  const location = useLocation();
  if (error) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "min-h-screen grid place-items-center p-4 bg-background", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center space-y-6 max-w-md", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "rounded-full bg-destructive/10 p-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "size-8 text-destructive" }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-xl font-semibold text-foreground mb-2", children: "Error de conexión" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm", children: "No se pudo establecer conexión con el servidor de autenticación." })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-muted/50 p-4 rounded-lg text-left space-y-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-mono text-destructive break-all", children: error.message }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border-t border-border pt-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-medium mb-2", children: "Posibles causas:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("ul", { className: "text-xs text-muted-foreground space-y-1 list-disc pl-4", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: "Variables de entorno de Supabase no configuradas" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: "El proyecto de Supabase no existe o está suspendido" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: "Problemas de red o CORS" })
          ] })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-3 justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { onClick: () => window.location.reload(), variant: "default", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "size-4 mr-2" }),
        "Reintentar"
      ] }) })
    ] }) });
  }
  if (loading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "min-h-screen grid place-items-center bg-background", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "animate-spin rounded-full h-10 w-10 border-4 border-primary border-t-transparent mx-auto mb-4" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-muted-foreground text-sm", children: "Verificando sesión..." })
    ] }) });
  }
  if (!session) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "min-h-screen grid place-items-center bg-background p-4", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center space-y-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-destructive text-lg font-semibold", children: "No autenticado" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm", children: "Inicia sesión para acceder al sistema contable." }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => window.location.href = "/login", children: "Ir al login" })
    ] }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(NotificationsProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(AlertSystemProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(DashboardShell, { session, location }) }) });
}
function DashboardShell({
  session,
  location
}) {
  const {
    expandedMode,
    toggleExpandedMode
  } = useUiPreferences();
  const canAdmin = usePermission("admin.usuarios");
  const canAuditoria = usePermission("admin.auditoria");
  const canPerformance = usePermission("admin.configuracion");
  const {
    sidebar,
    colores,
    isFeatureActive
  } = useEstudioConfig();
  const userEmail = session.user.email ?? "";
  const userName = session.user.user_metadata?.nombre ?? userEmail.split("@")[0] ?? "Usuario";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(AiComposerProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-screen flex bg-background font-sans", children: [
    !expandedMode && /* @__PURE__ */ jsxRuntimeExports.jsxs("aside", { className: "w-64 shrink-0 bg-sidebar text-sidebar-foreground flex flex-col shadow-premium-elevated border-r border-sidebar-border", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { type: "button", onClick: toggleExpandedMode, className: "p-6 flex items-center gap-3 border-b border-sidebar-border w-full text-left transition-all duration-300 hover:bg-sidebar-accent/50 group cursor-pointer", title: "Clic para activar Modo Expandido (pantalla completa)", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "size-10 rounded-xl bg-primary text-primary-foreground grid place-items-center font-display font-bold shadow-premium-medium pulse-glow group-hover:scale-105 transition-transform", children: "C" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "font-display text-lg font-semibold leading-none tracking-tight text-premium-gold", children: "CONTAM" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs text-sidebar-foreground/60 mt-1.5", children: "Sistema Contable" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("nav", { className: "flex-1 p-4 space-y-1", children: [
        nav.filter(({
          to
        }) => {
          if (!sidebar.modulos.includes(to)) return false;
          if (to === "/dashboard-estadisticas" && !sidebar.mostrar_dashboard_estadisticas) return false;
          if (to === "/sire-sync" && !sidebar.mostrar_estadisticas_sire) return false;
          return true;
        }).map(({
          to,
          label,
          icon: Icon
        }) => {
          const active = location.pathname.startsWith(to);
          return /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to, className: cn("relative flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm transition-all duration-300 ease-out", active ? "bg-primary text-primary-foreground font-medium shadow-premium-medium border border-primary/25" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground hover:translate-x-0.5"), children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: cn("size-4 shrink-0", active ? "text-primary-foreground" : "text-sidebar-foreground/50"), strokeWidth: 1.5 }),
            label,
            to === "/tareas" && /* @__PURE__ */ jsxRuntimeExports.jsx(SidebarAlertBadge, { type: "tasks" })
          ] }, to);
        }),
        canAdmin ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: "/admin/dashboard-estudio", className: cn("flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm transition-all duration-300 ease-out mt-2 border border-[#C8A95A]/20", location.pathname.startsWith("/admin/dashboard-estudio") ? "bg-[#C8A95A]/15 text-[#C8A95A] font-medium" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-[#C8A95A]"), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutDashboard, { className: "size-4 shrink-0", strokeWidth: 1.5 }),
          "Centro de Control"
        ] }) : null,
        canAdmin ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: adminNav.to, className: cn("flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm transition-all duration-300 ease-out mt-2 border border-[#C8A44D]/20", location.pathname.startsWith(adminNav.to) ? "bg-[#C8A44D]/20 text-[#C8A44D] font-medium" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-[#C8A44D]"), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Shield, { className: "size-4 shrink-0", strokeWidth: 1.5 }),
          adminNav.label
        ] }) : null,
        canAuditoria ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: auditoriaNav.to, className: cn("flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm transition-all duration-300 ease-out border border-[#00C8FF]/20", location.pathname.startsWith(auditoriaNav.to) ? "bg-[#00C8FF]/20 text-[#00C8FF] font-medium" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-[#00C8FF]"), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Shield, { className: "size-4 shrink-0", strokeWidth: 1.5 }),
          auditoriaNav.label
        ] }) : null,
        canPerformance ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: performanceNav.to, className: cn("flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm transition-all duration-300 ease-out border border-[#00C897]/20", location.pathname.startsWith(performanceNav.to) ? "bg-[#00C897]/20 text-[#00C897] font-medium" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-[#00C897]"), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Shield, { className: "size-4 shrink-0", strokeWidth: 1.5 }),
          performanceNav.label
        ] }) : null,
        canPerformance ? /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: configuracionNav.to, className: cn("flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm transition-all duration-300 ease-out border border-[#C8A95A]/20", location.pathname.startsWith(configuracionNav.to) ? "bg-[#C8A95A]/15 text-[#C8A95A] font-medium" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-[#C8A95A]"), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Settings, { className: "size-4 shrink-0", strokeWidth: 1.5 }),
          configuracionNav.label
        ] }) : null,
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Link, { to: "/notificaciones", className: cn("relative flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm transition-all duration-300 ease-out mt-2 border border-[#00D4FF]/15", location.pathname.startsWith("/notificaciones") ? "bg-[#00D4FF]/15 text-[#00D4FF] font-medium" : "text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-[#00D4FF]"), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Bell, { className: "size-4 shrink-0", strokeWidth: 1.5 }),
          "Notificaciones",
          /* @__PURE__ */ jsxRuntimeExports.jsx(SidebarAlertBadge, { type: "notifications" })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 border-t border-sidebar-border space-y-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "px-3 py-3 rounded-xl glass-surface text-xs", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "font-medium text-sidebar-foreground truncate", children: userName }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-sidebar-foreground/50 text-xs truncate mt-0.5", children: userEmail })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { variant: "ghost", size: "sm", className: "w-full justify-start text-sidebar-foreground/70 hover:bg-sidebar-accent hover:text-sidebar-foreground rounded-xl", onClick: async () => {
          await supabase.auth.signOut();
          window.location.href = "/login";
        }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(LogOut, { className: "size-4 mr-2" }),
          "Cerrar sesión"
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 flex flex-col min-w-0", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(AppTopBar, {}),
      /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "flex-1 overflow-x-hidden bg-background page-enter", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ContribuyentesProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(Outlet, {}) }) })
    ] }),
    expandedMode && /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { type: "button", onClick: toggleExpandedMode, className: "fixed top-4 left-4 z-40 flex items-center gap-2 rounded-xl bg-sidebar text-sidebar-foreground px-3 py-2 shadow-lg border border-sidebar-border hover:bg-sidebar-accent transition-colors cursor-pointer", title: "Clic para restaurar Modo Estándar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "size-8 rounded-lg bg-primary text-primary-foreground grid place-items-center font-display font-bold text-sm", children: "C" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-display text-sm font-semibold hidden sm:inline", children: "CONTAM" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ExpandedModeHint, {}),
    sidebar.mostrar_lupa_accesibilidad && /* @__PURE__ */ jsxRuntimeExports.jsx(AccessibilityLoupePremium, {}),
    colores.fab_visible && isFeatureActive("fab_alertas_dual") && /* @__PURE__ */ jsxRuntimeExports.jsx(FloatingAlertButtonPremium, {}),
    (sidebar.mostrar_chat_ai || isFeatureActive("chat_ia_contador")) && /* @__PURE__ */ jsxRuntimeExports.jsx(AiChatBubble, {})
  ] }) });
}
export {
  AppLayout as component
};
